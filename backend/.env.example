# 백엔드 환경변수 예시. 복사해서 .env 로 사용하세요.
# cp .env.example .env  (PowerShell: Copy-Item .env.example .env)

# 서버 포트
PORT=3000

# GPT API 키 (B06 AI 연동에서 사용). 코드/커밋에 직접 넣지 말 것.
OPENAI_API_KEY=
# 사용할 모델
OPENAI_MODEL=gpt-4o-mini
